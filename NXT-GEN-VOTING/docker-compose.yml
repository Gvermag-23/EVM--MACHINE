services:
  mongodb:
    image: mongo:7
    container_name: nxt-gen-voting-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: nxt_gen_voting
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
