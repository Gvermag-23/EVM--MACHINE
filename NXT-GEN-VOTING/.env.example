# Docker Compose configuration. `docker compose` reads this file from the repository root.
# The server reads `server/.env`, the client reads `client/.env` — keep them separate.

MONGO_PORT=27017
MONGO_INITDB_DATABASE=nxt_gen_voting